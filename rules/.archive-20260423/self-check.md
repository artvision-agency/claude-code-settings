# Самопроверка — топ ошибок

1. Потеря контекста → читай TODO.md + MEMORY.md ПЕРЕД вопросом пользователю
2. TaskCreate при старте → СРАЗУ, без напоминания, для КАЖДОЙ pending задачи
3. Фактчекинг → factcheck скрипт ПЕРЕД деплоем HTML клиенту. Не после, не потом
4. Агент без Bash бесполезен для деплоя → general-purpose для задач с scp/deploy
5. Записал правило ≠ сделал → каждое правило = хук/скрипт/автоматика
