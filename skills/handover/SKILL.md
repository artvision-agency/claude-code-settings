---
name: handover
description: Структурированная передача контекста между сессиями — генерирует HANDOVER.md с решениями+почему, изменениями, уроками, следующими шагами. Используй ПЕРЕД /compact, /clear или сменой контекста.
---

# /handover — структурированная передача сессии

> Цель: следующая сессия (или другой человек) поднимает работу за 30 секунд, не теряя ПОЧЕМУ были приняты решения.

## Когда использовать

| Триггер | Действие |
|---------|----------|
| Контекст ≥ 50% | СРАЗУ → handover → /compact focus |
| Закрытие сессии | handover → git push → закрыть |
| Смена задачи | handover → /clear → новая сессия |
| Перед /compact | handover ОБЯЗАТЕЛЕН (auto через pre-compact.sh) |
| Передача коллеге | handover → отправить путь к файлу |

## Где сохраняется

**Правило (Антон 30.05.2026): handover хранится в папке ПРОЕКТА, к которому относится сессия.**

| Тип сессии | Путь |
|-----------|------|
| **Клиент / проект** (работа по `clients/<slug>/` или `presales/<slug>/`) | `clients/<slug>/handover/HANDOVER-{YYYY-MM-DD-HHMM}-{topic}.md` — в **artvision-data** → git → видно всем 3 аккаунтам + Андрею |
| **Не-проектная** (infra / personal / general ops без клиента) | `~/.claude/handovers/HANDOVER-{YYYY-MM-DD-HHMM}-{context}.md` — глобально (claude-code-settings) |

**Хранение — до 10 handover на проект.** При 11-м — старейший перенести в `clients/<slug>/handover/archive/` (git хранит историю, активная папка не зашумляется при resume). Свежие лежат рядом с `context-log.md` / `STATUS.md` / `plan/task-plan*.md` → углубляют контекст при resume (правило `resume-read-project-state.md`).

**Почему в проекте, а не глобально (прецедент USmile 30.05):**
- Глобальная `~/.claude/handovers/` живёт в репо **claude-code-settings**, а данные клиента — в **artvision-data**. Клиентский handover в чужом репо → Андрей/второй аккаунт его НЕ видит при `git pull artvision-data` (split-brain).
- Проектный `HANDOVER.md` устаревал (27.05), пока свежий полный лежал только глобально (30.05) — рассинхрон.
- Handover рядом с канон-файлами проекта = больше деталей контекста при resume.

`{context}` для глобальных = `infra` / `personal` / `ops`-без-клиента (по cwd).
`.pending/` трекинг (маркеры по session_id) остаётся глобальным — это очередь «кому нужен handover», не содержимое.

## Структура HANDOVER.md (ОБЯЗАТЕЛЬНЫЕ секции)

```markdown
# Handover: {краткое название работы}

**Дата:** YYYY-MM-DD HH:MM
**Контекст:** {ops|bot|infra|presale|products}
**Сессия:** {имя сессии или session_id}
**Статус:** {в работе | заблокировано | завершено}

## 🎯 Цель сессии (одна строка)

{Что хотели достичь. Без воды.}

## ✅ Что сделано (с файлами + строками)

- `path/to/file.py:42` — добавил X, потому что Y
- `clients/blumart/kp/v3.html` — переписал блок ROI (старый = выдуманные числа)

## 🧠 Решения и ПОЧЕМУ (самая важная секция)

| Решение | Альтернатива | Почему выбрали это |
|---------|--------------|---------------------|
| {что выбрали} | {что отвергли} | {причина — данные, ограничения, опыт} |

> Пример:
> | Использовать Telethon вместо Bot API | python-telegram-bot | Bot API не даёт читать чужие чаты |

## ❌ Что НЕ сделано и почему

- {задача} — заблокировано: {причина}
- {задача} — отложено: {причина, когда вернуться}

## 📚 Уроки (новое знание для memory)

- {урок} → сохранить в `feedback_X.md` или `lessons-X.md`
- {урок} → обновить `MEMORY.md` строка X

## 🔜 Следующие шаги (приоритет)

1. **HIGH:** {что делать первым} — `/path/to/start.md`
2. MEDIUM: {что делать потом}
3. LOW: {nice to have}

## 🗺️ Карта файлов (где что лежит)

```
clients/blumart/
├── kp/v3.html          ← в работе
├── context-log.md      ← обновлён (запись от {дата})
└── meetings/           ← саммари встречи 14.04
```

## ⚠️ Гачи (gotchas — что знать перед стартом)

- VPS 147.45.232.226 → pm2 restart blumart-bot после деплоя
- factcheck-v2.py ОБЯЗАТЕЛЬНО перед scp в clients/
- Антон не любит "может быть" — давай прямо: "не знаю" / "проверю"

## 🔗 Связанные ресурсы

- Asana задача: {URL}
- TG обсуждение: {ссылка на сообщение}
- Предыдущий handover: `HANDOVER-{prev}.md`
```

## Алгоритм генерации

1. **Прочитать TODO.md** текущего контекста
2. **Прочитать context-log.md** клиента (если есть)
3. **git log --oneline -20** — что коммитил в этой сессии
4. **Восстановить решения** из истории (что выбрал, что отверг)
5. **Спросить себя:** что бы я хотел знать, поднимая эту работу через неделю?
6. **Определить путь по типу сессии** (см. «Где сохраняется»): клиент/проект → `clients/<slug>/handover/`; не-проектная → `~/.claude/handovers/`
7. **Записать** handover. В шапке — строка «Полное состояние → context-log.md + STATUS.md + plan/task-plan*.md (читать ПЕРВЫМ)» (правило `resume-read-project-state.md`)
8. **Ротация:** если в `clients/<slug>/handover/` уже >10 — старейший в `handover/archive/`
9. **Закоммитить** в нужный git: проектный → artvision-data; глобальный → claude-code-settings

## Различия с recap

| | Recap (built-in) | Handover (наш) |
|---|------------------|-----------------|
| **Когда** | Auto при /resume после idle | Вручную перед /compact /clear / закрытием |
| **Содержание** | Что делал последним | ПОЧЕМУ принял решения + следующие шаги |
| **Формат** | Авто-генерация LLM | Структурированный шаблон |
| **Назначение** | Я вспомнил контекст | Любой поднял работу |
| **Хранение** | В сессии | В git, переживает /clear |

## Различия с context-log.md

- **context-log.md** — лог КАЖДОГО действия (хук пишет автоматом)
- **handover.md** — СИНТЕЗ сессии (что важно, ПОЧЕМУ, что дальше)

Handover ссылается на context-log, не дублирует.

## Автоматизация

Хук `pre-compact.sh` (создать отдельно):
```bash
#!/bin/bash
# Перед /compact — напомнить про handover
echo "⚠️  Перед /compact создай HANDOVER через skill handover"
echo "   Иначе потеряешь контекст ПОЧЕМУ были приняты решения"
```

## Пример использования

```
Антон: давай заканчивать, /compact
Claude: СТОП. Сначала handover.

[создаёт ~/.claude/handovers/HANDOVER-2026-04-17-1845-ops.md]

Содержит:
- Сделано: BluMart KP v3, Atribeaute follow-up
- Решение: ROI убрали (фейковые числа) → используем "ожидаемая динамика"
- Урок: feedback_no_invented_metrics.md создан
- Дальше: послать BluMart, ждать ответа Atribeaute
- Гача: не публиковать без подтверждения Антона

Готово. Теперь можно /compact focus on "BluMart KP".
```

## Источник идеи

Zara Zhang (zara_zhang) на x.com — концепция структурированного handover для AI-агентов между сессиями. Адаптировано под Artvision workflow (5 контекстов, git, hooks).
