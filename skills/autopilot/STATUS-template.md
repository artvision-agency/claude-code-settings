# <Проект> — STATUS (autopilot pin)

> Живой пин autopilot. Свежая сессия после /clear читает ЭТОТ файл ПЕРВЫМ и продолжает ЭТУ тему (НЕ уходит в комбайн/меню). Обновлять ПО ХОДУ.
> Полное состояние → context-log.md + plan/task-plan*.md + последний handover (resume-read-project-state).
> Обновлён: <YYYY-MM-DD HH:MM> · Сессия: <session_id>

## ТЕМА (что добиваем — 1-2 строки)
<тема autopilot-сессии. Это якорь против дрейфа: свежая сессия видит ТЕМУ и продолжает именно её.>

## SCOPE (money-safe — HARD)
- ✅ whitelist (автономно, не тратит): assemble/build → upload --apply OFF/SUSPENDED → READ кабинета → файлы → git
- 🔒 blacklist (СТОП → «Ждёт Антона», CONFIRM Антона): включение (State=ON) / снятие с паузы / боевые ставки / ads.moderate / публикация страниц / платные API / контакт с клиентом / необратимое на проде

## ОЧЕРЕДЬ (живой TODO — сверху вниз, верхний = next)
- [ ] #1 <задача> — крит. готовности: <что значит done> [pipeline: <скрипт/скилл>]
- [ ] #2 <задача> — <крит.> [pipeline: <...>]
- [x] #0 <задача> — done <дата>, verify: <State=OFF / curl 200 / файл> [commit: <hash>]
- [🔒] #K <задача> — Ждёт Антона: <причина CONFIRM>

## СОСТОЯНИЕ (факты, обновлять после каждого шага)
- кабинет/файлы: <ключевые факты — какие кампании, статусы, артефакты>
- тратящих: 0 — подтверждено campaigns.get State на <дата> (числом, не «вроде»)

## ЖДЁТ АНТОНА (CONFIRM — собрать сюда ВСЁ blacklist-отложенное)
- <что> — <почему CONFIRM, что именно подтвердить>

## NEXT (одна строка — что делать первым при resume)
<верхний [ ] из ОЧЕРЕДИ ИЛИ «всё в Ждёт Антона → итог + handover → стоп»>
