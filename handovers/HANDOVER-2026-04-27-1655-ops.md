# Handover: TG-доступ + фиксация Хлыстова + проектирование Context Pipeline

**Дата:** 2026-04-27 16:55
**Контекст:** ops
**Сессия:** ce1911fc (cc-name: не задано)
**Статус:** Хлыстов закрыт, Pipeline отложен на новую сессию

## 🎯 Цель сессии

Антон спросил «можешь ли ты видеть переписку с человеком в TG?». Из этого выросло: (1) реальный экспорт чата с Хлыстовым, (2) фиксация Грелки-Гуделки в репо, (3) архитектурное обсуждение Context Pipeline (cron+детекторы событий+стратегия) — реализация отложена.

## ✅ Что сделано

- `clients/grelka-gudelka/presale/tg-history-2026-04-27.md` — экспорт 79 сообщений с @tonydanko (с маскировкой credentials)
- `clients/grelka-gudelka/presale/context-log.md` — дописан период 16.04 → 24.04 + TODO к запуску
- `clients/grelka-gudelka/access.md` — креды VK + Yandex Direct (gitignored, **не в репо**)
- `.gitignore:75-78` — добавлены паттерны `clients/*/access.md`, `clients/*/presale/access.md`, `clients/*/.env`
- Auto-sync hook закоммитил всё в commits `d30691ec9` (context-log) + `f5c17d1bc` (tg-history + .gitignore)
- CronCreate one-shot id `1100c811` на 2026-05-01 09:03 — pings me about Хлыстов follow-up
- TaskCreate #1-#6 — top high tasks из TODO + новый task #6 (TG-export сервис)
- `/Users/antonk/.claude_temp_scripts/tg_search_khlystov.py` + `tg_export_khlystov.py` — рабочие one-shot скрипты, паттерн для будущего pipeline

## 🧠 Решения и ПОЧЕМУ

| Решение | Альтернатива | Почему выбрали |
|---------|--------------|-----------------|
| Telethon (userbot) | Bot API + добавить бота в чат | Bot API не видит DM с третьими лицами; userbot читает всё что вижу я лично, без следа для собеседника |
| Копия `.session` в `/tmp` | Остановить tg-listener daemon на 30 сек | tg-listener держит exclusive SQLite-lock; cp session = read без конфликта (риск: auth_key duplication, но 60 сек read-only безопасны) |
| НЕ использовать Codex для плана | Codex (GPT-5.4) | Codex стоит денег (OpenAI API), для планирования не оправдан; план сделаю сам через `superpowers:brainstorming` + `writing-plans`; round_table если нужен второй взгляд (FREE) |
| `access.md` в gitignore | Хранить в `tokens.json` или 1Password | Паттерн уже описан в `client-docs-storage.md` («access.md = исключение, .gitignore при необходимости»); просто и работает; добавил паттерн глобально для всех клиентов |
| CronCreate session-only | launchctl plist / system cron | system cron надёжнее, но overkill для одного reminder; в TODO.md есть due:2026-05-01 как fallback (через SessionStart hook) — НЕ ДОДЕЛАНО, см. ниже |
| Context Pipeline отложить | Сразу начать реализацию | Контекст ушёл за 80%, Dumb Zone глубокий; 19ч работы в плохом контексте = плохой код; правильно — handover + новая сессия с brainstorming |

## ❌ Что НЕ сделано

- **TODO fallback на 01.05** в `artvision-data/presale/TODO.md` — НЕ дописал. CronCreate session-only, если Claude не работает 01.05 утром — ремайндер пропадёт. **Сделать первым шагом следующей сессии.**
- **Cabinet audit** Грелки — ждёт пока Антон пропишет `borisovaloves@yandex.ru` в Метрику и Директ клиента. Запустить когда даст пинг.
- **Context Pipeline реализация** — 5 компонентов, ~19ч (auto-export, event detector, aggregator, daily digest, event→skill router). Полностью отложено.
- **`/tmp/tg-khlystov-export.md`** — оставлен на диске с credentials в plain text. Антон должен `rm` когда не нужен.
- **Task #6 не переформулирован** — должен быть «решить session-lock + productize», а не «сделать экспорт» (он уже работает).

## 📚 Уроки

- **Auto-sync hook через ~1 минуту** разбивает мои изменения на разные коммиты в зависимости от тайминга Edit/Write. Не страшно, но `git status` сразу после Edit может показать «clean» — это значит уже закоммичено, а не пропало. → НЕ создавать новый feedback, известное поведение.
- **Telethon SQLite session = exclusive lock**. Параллельный доступ → `database is locked`. Workaround `cp session_file → use copy` — read-only OK, но writes (Telegram session refresh) могут вызвать invalidation. → Кандидат в `feedback_telethon_session_lock.md` если повторится.
- **Имя клиента в репо ≠ имя контакта в TG.** Хлыстов искался grep'ом по `clients/` → пусто, потому что папка называется `grelka-gudelka` (по проекту). Правильный поиск — через TG dialog search или через метаданные клиента (`access.md`/`README.md` со списком контактов). → Стоит добавить в `clients/<name>/README.md` секцию «Контакты с TG @username» для будущих grep'ов.

## 🔜 Следующие шаги

1. **HIGH (СРАЗУ в новой сессии):** добавить запись в `presale/TODO.md` с `[client:grelka-gudelka] [due:2026-05-01] [priority:high] [assignee:claude]` — fallback к Cron-reminder
2. **HIGH (когда Антон даст пинг):** cabinet audit Грелки — ВК таргет history + Директ history + Метрика baseline + сверка с `presale/audit/competitors.md`
3. **MEDIUM (новая чистая сессия):** Context Pipeline — `superpowers:brainstorming` → план → опционально `round_table` (FREE) → реализация по компонентам
4. **MEDIUM:** TaskUpdate #6 — переформулировать на «Productize TG-export: вторая Telethon-сессия ИЛИ wrapper-скрипт `tg-chat-export-safe.sh` без `.session` копии в /tmp»
5. **LOW:** Антону — `rm /tmp/tg-khlystov-export.md` после проверки `clients/grelka-gudelka/access.md`

## 🗺️ Карта файлов

```
artvision-data/
├── .gitignore                                  ← +clients/*/access.md (line 75-78)
├── clients/grelka-gudelka/
│   ├── access.md                               ← НОВЫЙ, gitignored, креды VK+Direct
│   ├── context-log.md                          ← старое (без изменений)
│   └── presale/
│       ├── context-log.md                      ← обновлён (16-24.04 events)
│       ├── tg-history-2026-04-27.md            ← НОВЫЙ, 79 сообщений
│       ├── audit/action-plan.md                ← готовый план 5 стадий, бюджет 90-260к/мес
│       └── kp/grelka_kp.html                   ← КП (уже у клиента)

~/.claude/
├── scripts/tg-chat-export.py                   ← локальный экспортер (single-process)
├── skills/tg-chat-export/SKILL.md              ← документация скрипта
└── handovers/HANDOVER-2026-04-27-1655-ops.md   ← этот файл

~/.claude_temp_scripts/
├── tg_search_khlystov.py                       ← образец: поиск диалога через копию session
└── tg_export_khlystov.py                       ← образец: экспорт через копию session

/tmp/
└── tg-khlystov-export.md                       ← ⚠️ полный дамп с кредами, удалить вручную
```

## ⚠️ Gotchas

- **`pre-tool-block-no-taskcreate.sh`** (Layer 2 hook от 27.04, активирован сегодня) блокирует все Edit/Write/Bash до первого TaskCreate в сессии. В новой сессии — TaskCreate top-5 high из TODO ПЕРВЫМ ДЕЛОМ, иначе будет ругаться.
- **`pre-bash-resource-guard.sh`** активен (RAM/диск guard) — большие команды могут блокироваться.
- **`pro.artvision.tg-listener` daemon** держит `~/.claude/state/telethon_session.session` exclusive. НЕ пытайся `launchctl stop` — launchd рестартит. Использовать `cp session` или productize через task #6.
- **Auto-sync hook** коммитит часто — НЕ беспокоиться если `git status` показывает clean сразу после Edit.
- **CronCreate `durable: true` фактически session-only** — runtime ответил «not written to disk». Не полагайся на него для долгих reminder'ов.
- **Антон не любит «магические числа» без источника** — challenge-self хук ловит regex `\d+[-–]\d+%` и инжектит skill `challenge-self` в следующий turn.
- **Хлыстов = @tonydanko = ID 394777236.** Папка клиента названа `grelka-gudelka` (по проекту). Этот mapping важен для следующих grep'ов.

## 🔗 Связанные ресурсы

- TG: @tonydanko (Anton Khlystov, ID 394777236)
- Action plan: `clients/grelka-gudelka/presale/audit/action-plan.md`
- KP: `clients/grelka-gudelka/presale/kp/grelka_kp.html`
- Senior review: `clients/grelka-gudelka/presale/kp/senior-review-2026-04-10.md`
- Реестр клиентов: `artvision-data/.claude/rules/clients-registry.md` (Грелка в «Presale — КП отправлено»)
- Cron job: id `1100c811` (one-shot 2026-05-01 09:03, session-only)
- Tasks в текущей сессии: #1-#6 (см. TaskList)
- Recap файл сессии: `sync/recaps/4f04feb3-62a9-4485-8457-350082eea12c.md` (нужно дозаполнить)
