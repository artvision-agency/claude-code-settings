# Handover: фикс TODO-инжектов + старт sub-goal «500 трафика по рекламной нише»

**Дата:** 2026-04-27 21:37
**Контекст:** ops (формально cwd=~, работа в `~/.claude` + `~/artvision-data`)
**Сессия:** безымянная (cc-name не выставлен)
**Статус:** в работе — Phase 1 интервью у нового goal заполнен наполовину, Phase 2/3 НЕ начаты (контекст 90%)

## 🎯 Цель сессии

(1) Антон попросил «доделать висящие задачи по сайту artvision.pro»; (2) уточнил «фикс хука TODO-таскрейта — должен фильтровать по теме сессии, а не выводить всё»; (3) затем запустил `/goal получить 500 трафика по коммерческим и инфо ключам по рекламе в интернете` — это и стало финальным фокусом.

## ✅ Что сделано

### A. Фикс хуков фильтрации TODO по контексту (закрыто, запушено)

- `~/.claude/scripts/lib/todo-route.sh` — добавил явную ветку `cwd=$HOME` → `ctx=home, todo=""` (раньше падало в default ops fallback, что мешало хукам различать «явный artvision-data» и «дом без проекта»)
- `~/.claude/hooks/start-todo-tasks.sh` — переписан на `todo_for_cwd`, инжектит top-5 high ТОЛЬКО из TODO текущего контекста; `ctx=home` → silent
- `~/.claude/hooks/prompt-taskcreate-nag.sh` — переписан на `todo_for_cwd`; `cwd` теперь берётся из stdin JSON (надёжнее `$PWD` в дочернем shell хука); `ctx=home` → silent
- `~/.claude/scripts/session-menu.sh` — добавлены ветки `presale`/`products` в case (раньше падали в `—`); счётчик «📊 Очередь (X)» и «🎯 Top (X)» теперь показывают только текущий контекст; сводка 5 контекстов осталась только для `cwd=$HOME`
- Тесты прогнаны для всех 6 кейсов (home/ops/bot/infra/presale/products) — все правильно маршрутизируются. Для infra silent — нет priority:high в TODO, это норма (fallback на первые 5 pending можно добавить если нужно).
- Зеркалирование в `~/claude-code-settings`, commit `f62f624`, push `2d8bd1a` (после rebase). **Файлы под git, переживают /clear.**

### B. Pre-task scan по artvision.pro

- Нашёл секцию `## artvision.pro — сайт` в `~/artvision-data/PROJECTS.md:253` и `~/artvision-data/TODO.md:212`
- 6 pending по сайту (см. ниже Решения)
- `/free-audit/*` 404 — масштаб проверен: 3 ссылки на главной, в footer всех страниц, в sitemap, в блоге. На VPS все 3 страницы возвращают 404. `llms.txt` уже исправлен (Free SEO Audit → /contacts/), но nav/footer/sitemap нет.
- Schema-аудит `seo/artvision-pro/schema-markup-2026-03.md` ссылается на старые `/uslugi/*` URL (миграция 24.03 переехала на `/seo-prodvizhenie-sayta/` и т.п.) — нужен пере-аудит на живые URL.

### C. Sub-goal создан

- `~/artvision-data/goals/GOAL_500-traffic-ads-niche_2026-04-27.md` (новый, статус INTERVIEW)
- Родитель: `GOAL_500-visitors-day-artvision_2026-03-18.md` (ACTIVE, 500/день в целом, дедлайн 18.04 просрочен 9 дней)
- Phase 0 (якорь-цитата) ✅, Phase 1 интервью — 2/9 ответов есть («ASAP» + «скорость»), 7 вопросов с дефолтами TBD
- Phase 2 (path) и Phase 3 (execute) НЕ начаты — контекст 90%, Dumb Zone

## 🧠 Решения и ПОЧЕМУ

| Решение | Альтернатива | Почему выбрали это |
|---------|--------------|---------------------|
| Явная ветка `cwd=$HOME` в `todo-route.sh` (ctx=home, todo="") | Сделать всю проверку в каждом хуке отдельно через `if [ "$PWD" = "$HOME" ]` | Один источник истины — всё что использует `todo_for_cwd` (combine, sync-sessions, хуки) теперь различает home корректно, без дублирования |
| `prompt-taskcreate-nag.sh` берёт cwd из stdin JSON, не из `$PWD` | Использовать `$PWD` как раньше | UserPromptSubmit-хук получает session info через stdin — это документированный канал. `$PWD` в дочернем shell может отличаться (тест showed bug) |
| Sub-goal `GOAL_500-traffic-ads-niche` вместо обновления родительского | Обновить `GOAL_500-visitors-day-artvision` с детализацией | (1) родитель уже просрочен и тематически шире (всё SEO); (2) новая цель — нишевая (только реклама-в-интернете); (3) сохраняется audit trail двух разных подходов |
| НЕ действовать на /free-audit/ 404 без Антона | Сразу 301 или сразу удалить из nav/sitemap | Это 5.3% PR (по `page-power-2026-03-19.md`) — бизнес-решение, требует CONFIRM по `security.md` (публичный сайт нашего агентства) |
| Прервать /goal на Phase 1 при контексте 90% | Продолжить Path+Execute в этой же сессии | Bulletproof 40% rule — после 80% /compact теряет важное, в Dumb Zone детали path исчезнут |

## ❌ Что НЕ сделано и почему

- **Phase 2 (Path) и Phase 3 (Execute) для нового goal** — отложено до /clear. Контекст 90%, Dumb Zone. **Это главное что висит.**
- **Schema-аудит artvision.pro** — отложено: ~1.5-2ч работы, не уместится в текущий контекст
- **`/free-audit/` fix** — заблокировано: нужно решение Антона A/B/C (восстановить контент / убрать из nav+sitemap / 301 на /contacts)
- **CRO-план** (`seo/artvision-pro/cro-plan-2026-04-24.md`) — ждёт approve Антона (не моя зона)
- **QR на страницах услуг** — нужно уточнение Антона (визитка/оплата/TG)
- **Clutch.co + 3 кейса EN** — `priority:low`, отложено
- **app-passwords почт artvision.pro** — `assignee:anton`, не я
- 4 из 5 top-high задач из стартового меню (Дентикс, UNOtrans, BluMart×2) — `assignee:anton` или контакт клиента (CONFIRM-зона по `security.md`). Помечены в Phase 0 первой ветки сессии.

## 📚 Уроки

- **264 pending во всех 5 TODO одновременно инжектились в каждую сессию** — нерелевантный спам, мешал salience. Теперь фильтр по cwd обязателен. Если появятся новые TODO-хуки — использовать `todo-route.sh` как источник истины. Записать в `feedback_hook_context_filtering.md` (TBD в next session).
- **`PWD=value command` в bash inline НЕ переопределяет `$PWD` в дочернем shell** — bash при старте перезаписывает на реальный pwd. Для тестов хуков использовать `(cd path && cmd)` или JSON stdin.
- **/goal на 79%+ контексте — стоп после Phase 1.** Любой Path/Execute теряет детали при /compact. По правилу bulletproof.md 40% rule — рекомендация: handover→/clear перед Phase 2.

## 🔜 Следующие шаги (приоритет)

1. **HIGH (после /clear):** Открыть `~/artvision-data/goals/GOAL_500-traffic-ads-niche_2026-04-27.md`, дозаполнить Phase 1 (вопросы 2-7, 9 — есть TBD-дефолты которые можно подтвердить или сменить), затем Phase 2 (path) + Phase 3 (execute). Стартовая команда:
   ```bash
   cd ~/artvision-data && cat goals/GOAL_500-traffic-ads-niche_2026-04-27.md
   ```
2. **MEDIUM:** Решение Антона по `/free-audit/`: A (восстановить 3 страницы) / B (убрать из nav + sitemap) / C (301-редирект). После выбора — выполнить.
3. **MEDIUM:** Пере-аудит Schema (`seo/artvision-pro/schema-markup-2026-03.md` устарел). ~1.5-2ч. Делается в отдельной сессии.
4. **LOW:** CRO-план approve, QR-уточнение, Clutch.co — ждут Антона.

## 🗺️ Карта файлов

```
~/.claude/
├── hooks/start-todo-tasks.sh       ← переписан на todo_for_cwd
├── hooks/prompt-taskcreate-nag.sh  ← переписан, cwd из stdin
├── scripts/session-menu.sh         ← presale/products + фильтр top
├── scripts/lib/todo-route.sh       ← +ветка home
└── handovers/HANDOVER-2026-04-27-2137-ops.md  ← этот файл

~/claude-code-settings/             ← зеркало, commit f62f624 → push 2d8bd1a

~/artvision-data/
├── goals/GOAL_500-traffic-ads-niche_2026-04-27.md  ← НОВЫЙ, INTERVIEW phase
├── goals/GOAL_500-visitors-day-artvision_2026-03-18.md  ← родитель, ACTIVE, просрочен
├── TODO.md:212                     ← секция artvision.pro — сайт (6 pending)
├── PROJECTS.md:253                 ← секция artvision.pro — сайт (pipeline)
├── clients/artvision-pro/          ← CLAUDE.md прочитан, patches/ пуст, context-log до 24.03
└── seo/artvision-pro/              ← schema-markup-2026-03.md устарел, cro-plan-2026-04-24.md ждёт approve
```

## ⚠️ Гачи

- **Контекст 90% при /clear** — этот handover + GOAL-файл в git. После /clear: `cd ~/artvision-data && git pull` (на всякий случай) → читать GOAL → продолжать с Phase 2.
- **Hook test bug:** `PWD=path command` НЕ работает для тестов хуков. Используй `(cd path && command)` или JSON через stdin.
- **5 TaskCreate в этой сессии** были созданы и затем **deleted** — они отражали top-5 high из старого нефильтрованного хука (4 из 5 на Антоне, не actionable для меня). После /clear сессия в `~/artvision-data` получит уже отфильтрованный список (только ops, top-5 ops-high) — TaskCreate будет уместен.
- **Переписка с клиентом на этапе execute = CONFIRM** (security.md).
- **Если возьмёшь /free-audit/ fix** — НЕ менять nav/sitemap без Антона (5.3% PR), но добавить 301-редирект на VPS можно без бизнес-impact как временное решение если согласовано.

## 🔗 Связанные ресурсы

- Commit фикса: `~/claude-code-settings` → `f62f624` (push `2d8bd1a`)
- Родительский handover для общей цели 500/день: `goals/GOAL_500-visitors-day-artvision_2026-03-18.md` (там же есть лог по Шагам 1-6 DONE, 4-5-7-8 в работе)
- TODO секция: `~/artvision-data/TODO.md:212-225`
- 9 вопросов интервью: `~/artvision-data/goals/GOAL_500-traffic-ads-niche_2026-04-27.md` (Phase 1 таблица)
