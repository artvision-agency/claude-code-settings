# Handover: Otido (eLama) — баланс, дни откруток, последний платёж

**Дата:** 2026-04-27 19:04
**Контекст:** ops
**Сессия:** `0f9716e5-1d81-4421-970f-04a8205a30c5` (153 мин, 39 substantive calls)
**Статус:** ✅ завершено

## 🎯 Цель сессии

Понять состояние Direct-кабинета Отидо в eLama: баланс, дни с откруткой за 30 дней, был ли последний платёж и хватает ли его.

## ✅ Что сделано

- Найден sub-client Отидо: eLama агентка `artvisionelama-e2` → клиент `e-20041775` (ClientId `109996867`)
- 22 не-архивных кампании — все OFF, кроме `707945609 РСЯ/TG-лендинг/МСК+СПб` (SUSPENDED)
- Через Direct Reports API (`CAMPAIGN_PERFORMANCE_REPORT`, 28.03–27.04): 28/31 дней с расходом, 3 без (01.04, 19.04, **27.04**)
- Сумма за 30 дней: **172 952,65 ₽ с НДС**, показы 235 162, клики 4 115
- Паттерн: до 23.04 ровно ~7 800 ₽/день → 24–26.04 копейки → 27.04 = 0 (бюджет исчерпан)
- Источник правды по платежу — `scripts/.otido_balance_state.json`: счёт `INV-OTIDO-20260319-0125` от **18.03.2026 на 50 000 ₽**, статус `pending` (НЕ оплачен **40 дней**)
- Текущий баланс кабинета: **0 ₽**
- Cron-скрипт `otido_balance_checker.py` шлёт алерты, но TG-бот `8399522625` заблокирован у получателя (403 Forbidden, 204 алертов в очереди)

## 🧠 Решения и ПОЧЕМУ

| Решение | Альтернатива | Почему |
|---------|--------------|--------|
| Искать счёт через `scripts/.otido_balance_state.json` | Парсить почту (dune87@, boss@artvision.pro) | В 2 ящиках за 60 дней 0 платёжных писем от eLama. Локальный state cron-скрипта оказался единственным источником истины |
| Reports API CAMPAIGN_PERFORMANCE_REPORT | Stat API / UI скрин | Reports API даёт TSV per-day по всем кампаниям сразу — агрегация за секунды |
| Не лезть в eLama UI без необходимости | Открывать кабинет вручную | Все нужные данные собрались через API + state-файл, UI избыточен |

## ❌ Что НЕ сделано / зависло

- Алерты в TG: бот `8399522625` заблокирован получателем → 204 неотправленных. **Вопрос Антону:** разблокировать бота или сменить chat_id?
- Счёт `INV-OTIDO-20260319-0125` так и не оплачен — это **операционный блокер клиента**, не технический

## 📚 Уроки (для memory)

- **При вопросе про баланс/платёж клиента — СНАЧАЛА `scripts/*balance*` и `*.state.json`, потом почта.** Cron-скрипты часто ведут локальный state, который точнее почты. → нужен `feedback_check_state_files_before_email.md`
- **eLama sub-clients** ищутся в агентских аккаунтах через `agencyclients/get`, ClientId ≠ Login (107996867 vs `e-20041775`)

## 🔜 Следующие шаги

1. **HIGH:** проверить с Антоном статус счёта `INV-OTIDO-20260319-0125` (50к pending 40 дней) — нужна оплата или разговор с Otido
2. **HIGH:** разблокировать TG-бота `8399522625` у получателя или сменить алерт-канал (204 алертов в очереди)
3. MEDIUM: записать урок про state-files в `feedback_check_state_files_before_email.md`

## 🗺️ Карта файлов

```
artvision-data/
├── scripts/
│   ├── otido_balance_checker.py        ← cron, шлёт алерты в TG
│   └── .otido_balance_state.json       ← ★ source of truth по балансу/счёту
└── tokens.json                          ← Direct: artvisionelama-e2 (агентка)
```

## ⚠️ Гачи

- eLama sub-client login имеет префикс `e-` (`e-20041775`), это НЕ ClientId
- Direct Reports API требует заголовок `Client-Login: <sub-client>` при запросе через агентский OAuth
- Бот `8399522625` заблокирован → не использовать без разблокировки

## 🔗 Связанные

- Recap: `artvision-data/sync/recaps/0f9716e5-1d81-4421-970f-04a8205a30c5.md`
- Pending markers зачищены (10 шт. → 0)
