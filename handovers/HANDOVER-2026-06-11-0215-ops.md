# Handover: SMM-стек с GitHub → tool-research workflow + provenance

**Дата:** 2026-06-11 02:15
**Контекст:** ops (инфра/процессы, не клиент)
**Сессия:** c60a6f3a-ae82-420c-b100-90fa67191b72
**Статус:** завершено (CLOSED), осталось итеративное

> Полное состояние → research/2026-06-10-smm-github-tools.md + sync/recaps/c60a6f3a-...md (читать ПЕРВЫМ).

## 🎯 Цель сессии (одна строка)

Команда рисерчеров нашла на GitHub проверенные SMM-инструменты по 5 направлениям → консолидация → adoption-вердикт → закрепление как named workflow → provenance (авторство генерации по моделям).

## ✅ Что сделано (с файлами)

- `research/2026-06-10-smm-github-tools.md` + `.html` — консолидированный отчёт (3 research-агента: графика/форматы/InstantView/сайт→БД/sales). Deploy: **artvision.pro/_priv-smm-github-tools-2026-06-10/** (HTTP 200).
- `.claude/workflows/tool-research.js` — named workflow «команда рисерчеров» (fan-out ≤4 → консолидация → пост-шаги md+HTML+round_table+git). Routing-строка добавлена в `CLAUDE.md`.
- `templates/social-sizes.yaml` — справочник размеров TG/VK/IG/YT (TG CONFIRMED, VK/IG UNCONFIRMED до сверки).
- `templates/sales-copywriting-frameworks.md` — 25 продающих формул + 15 хуков (из fabric/CoppieGPT/linexjlin-GPTs), 27КБ.
- `scripts/smm/site_to_project_db.py` — glue-скрипт crawl4ai «сайт→БД проекта» + **circuit breaker** (3 антибот-фейла → requests-fallback). Пилоты: `clients/usmile/smm/project-db.json` (131 услуга, 20 цен ✅), `clients/tvorimsovershenstvo/smm/project-db.json` (146 услуг, цены частично).
- `.claude/rules/model-bakeoff.md §PROVENANCE` (глоб + sync artvision-data) — авторство генерации у каждого артефакта.
- Коммиты: `1e56adb2e2`, `46319fce78`, `f639bed0f3`, `e4b2df1eec`, `1737aa6643` (+ глоб `2cdebbd4`).

## 🧠 Решения и ПОЧЕМУ

| Решение | Альтернатива | Почему |
|---|---|---|
| satori ✅ внедрять, postiz ❌ | взять postiz как планировщик | round_table (llama+qwen3+gpt-oss): postiz дублирует нативный отложенный постинг TG + нет VK; satori быстрее Playwright в batch |
| свой `social-sizes.yaml` | форкнуть GitHub-репо размеров | репо >1k★ со свежими размерами НЕ существует (все мертвы, содержат Google+) — честный negative |
| sales-формулы дистиллировать в свой файл | адоптить awesome-marketing | репо sales-frameworks >1k★ не существует; fabric/CoppieGPT <1k но контент ценный |
| circuit breaker в скрипте | долбить crawl4ai на антибот-сайте | Творим валил crawl4ai (ACS-GOTO ×30с/стр → глобальный таймаут) |
| handover в глобальную папку | clients/<slug>/handover/ | сессия не про клиента (инфра/процессы) |

## ❌ Что НЕ сделано и почему

- Установка satori с кириллическими TTF — отложено: отдельная задача когда дойдём до batch-генерации карточек.
- Полная проверка ~20 репо звёзд токеном — частично: выборка 27/27 через GitHub API подтвердила, 100% не критично.
- Экстракторы цен под нестандартные вёрстки — итеративное: на Творим цены/адрес собрались частично (вёрстка не табличная).

## 📚 Уроки (для memory/self-corrections)

- **PROVENANCE ловит автора, но НЕ ловит галлюцинацию данных.** Strict factcheck нашёл `polotno-project/polotno-studio 1.3k★` — **репо не существует** (404). WebFetch-«подтверждение» субагента это пропустило. 26/27 верны (~96%). → Числовые факты субагентов (звёзды/цены/метрики) перепроверять детерминированным источником (GitHub API с токеном `github.primary.token`, rate 5000/ч), не WebFetch. Записано в `model-bakeoff.md` (Урок 2026-06-11).
- Не тестировать скрипты пайпом `| grep | tail` — EPIPE убивает Playwright-драйвер. Лог в файл.

## 🔜 Следующие шаги (приоритет)

1. **HIGH:** установить satori + кириллические TTF → batch-генерация карточек постов (правило: round_table уже PASS).
2. MEDIUM: glue-скрипт `site_to_project_db.py` — дорабатывать экстракторы цен под новые CMS по мере применения; на новом клиенте `project-db.json` проверять глазами (prices/contacts).
3. LOW: VK/IG размеры в `social-sizes.yaml` сверить с офиц. доками (сейчас UNCONFIRMED).
4. LOW: дистиллировать fabric-паттерны в сам скилл `/smm-strategist` (сейчас только reference-файл).

## 🗺️ Карта файлов

```
artvision-data/
├── research/2026-06-10-smm-github-tools.{md,html}   ← главный отчёт + strict-вердикт
├── .claude/workflows/tool-research.js                ← named workflow
├── .claude/rules/model-bakeoff.md                    ← §PROVENANCE (sync глоб)
├── templates/social-sizes.yaml
├── templates/sales-copywriting-frameworks.md
├── scripts/smm/site_to_project_db.py                 ← glue crawl4ai + circuit breaker
└── clients/{usmile,tvorimsovershenstvo}/smm/project-db.json  ← пилоты
~/.claude/rules/model-bakeoff.md                      ← глобальная копия §PROVENANCE
```

## ⚠️ Гачи

- GitHub-токен для перепроверки звёзд: `tokens.json → github.primary.token` (gho_, scopes repo/workflow). Без токена API даёт N/A (rate-limit).
- crawl4ai установлен (0.8.9). На антибот-сайтах circuit breaker → requests-fallback (цены собираются хуже).
- Вызов workflow: «команда рисерчеров <тема>» ИЛИ `Workflow({name:'tool-research', args:{topic, directions, stars_min, adoption, deploy}})`.
- Контекст этой сессии дошёл до 86% — поэтому handover + /clear, не /compact.

## 🔗 Связанные ресурсы

- Deploy: https://artvision.pro/_priv-smm-github-tools-2026-06-10/
- Recap: `sync/recaps/c60a6f3a-ae82-420c-b100-90fa67191b72.md` (CLOSED)
- Правила: model-bakeoff.md §PROVENANCE, tool-adoption-proof.md, proven-tools-first.md
