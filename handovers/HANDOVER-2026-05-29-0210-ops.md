# Handover: рефакторинг repo artvision-data (4 фазы + security-находка)

**Дата:** 2026-05-29 02:10 МСК
**Контекст:** ops
**Сессия:** 6f79d39e-fcfa-4594-81f0-5a8e5c392106
**Статус:** PARTIAL — 4 одобренные фазы завершены, HIGH-risk отложен (task #6)

## 🎯 Цель сессии

Рефакторинг repo artvision-data (Антон выбрал scope «весь repo» → за ночь сделаны 4 безопасные обратимые фазы, HIGH-risk вынесен).

## ✅ Что сделано (всё запушено)

- `artvision-data` `a4810c4f` — Phase 3 Security: untrack 5 `tokens.json.bak*` (секреты) + `.gitignore` паттерны. Локальные файлы целы (`--cached`).
- `artvision-data` `8e287402` — Phase 1 Hygiene: untrack 142 файла (133 `.bak` + 2 `.pyc` + `tools/rag/chroma_db/` 25MB регенерируемый).
- `~/.claude` `ba243f7b` — Phase 2 orm-pulse:
  - `skills/orm-pulse/scripts/lib/config.py:7` — `ROOT = os.environ.get("ORM_ROOT", <fallback>)` (чинит VPS, fallback сохраняет cron'ы).
  - `skills/orm-pulse/scripts/orders-state.py` — добавил `published_breakdown()`, заменил раздутую строку «Опубликовано 42» на честную разбивку live_verified(нижняя)/Sheet-заявлено(верхняя).
- `~/.claude` `4356cf9c` — БОНУС: `hooks/stop-sync-all.sh:171` — добавил `tokens.json*`/`*.bak` в `git reset HEAD` исключения.
- `artvision-data/audits/2026-05-29-repo-refactor/orphan-candidates.md` — Phase 4: 141 orphan-кандидат (отчёт, НЕ удаление).

## 🧠 Решения и ПОЧЕМУ

| Решение | Альтернатива | Почему |
|---------|--------------|--------|
| 4 фазы за ночь, HIGH-risk отложить | «рефактори весь repo» | 16GB/22800 файлов/3 клона+VPS — не одна сессия; HIGH-risk нужна координация |
| gitignore-first перед `git rm --cached` | просто `git rm --cached` | stop-sync-all `git add -A` re-добавлял on-disk файлы — gitignore блокирует re-add |
| orders-state: честная разбивка (диапазон), НЕ автоматический union | реализовать union(live+pusk+OCR) | attribution fuzzy недооценивает (3), Sheet переоценивает (190); union = false precision, реальное число даёт только ручной разбор (strict 23.05 = 22-25) |
| Патч хука как defense-in-depth | оставить только .gitignore | .gitignore — основная защита; патч на случай force-add/обхода |
| НЕ трогать avtoworld-удаления | закоммитить/откатить | не моё (удалены до сессии), уже авто-запушены хуком |

## ❌ Что НЕ сделано (task #6, нужна координация + явный go)

- 🔴 **Ротация утёкших ключей** — `tokens.json.bak*` были в истории git на 3 remote. Untrack остановил будущие коммиты, НО старые в истории. Нужна: ротация реальных API-ключей в сервисах + `git-filter-repo` чистка истории + force-push на 3 remote + re-clone VPS.
- 🔴 Шринк `.git` 3.3GB (история бинарей) — git-filter-repo, ломает 3 клона + VPS.
- 🟠 Реорг ~90 top-level директорий — churn, скрипты завязаны на пути.
- 🟡 Orphan 2-й проход — отсканировать session-логи/docs/memory/git log чтобы отсечь false-positives из 141.
- 🟡 `temp/kp-shanti.html` (354KB, реальное КП mebel-shanti) + `tmp/llms-txt/*` (GEO-деливераблы) — переместить в `clients/`, НЕ untrack.

## 📚 Уроки (для memory)

- **stop-sync-all.sh пушил секреты** (корень self-corrections #20): Stop-хук `git add -A` + push на 3 remote каждый turn, исключения секретов не покрывали `tokens.json`/`.bak`. → дополнить self-corrections #20.
- Авто-коммиттер каждый turn = помеха атомарным reviewed-коммитам при рефакторинге. Обход — gitignore-first.
- orders-state «published» нельзя считать автоматически точно — нужен диапазон, не число.

## 🔜 Следующие шаги (приоритет)

1. **HIGH (но координация):** task #6 п.2 — ротация ключей + git-filter-repo. НЕ автономно: остановить sync на 3 аккаунтах, rewrite, force-push, re-clone VPS.
2. MEDIUM: orphan 2-й проход (отсечь FP) → архивация подтверждённых в `_archive/scripts/`.
3. LOW: перенос tmp/temp деливераблов, проверка дублей rules.

## ⚠️ Гачи

- `stop-sync-all.sh` (Stop-хук) пушит artvision-data + ~/.claude каждый turn `git add -A` — мои «hold push» moot, всё уходит само.
- orm-pulse скрипты в `~/.claude` (claude-code-settings repo), НЕ artvision-data — коммитить туда.
- 10 LaunchAgent `blumart-*` запускают orm-pulse скрипты локально — config.py fallback не ломает их.
- `git push ~/.claude` ловит network disconnect (vpn-watchdog) — retry helps, «Everything up-to-date» = прошло.

## 🔗 Связанные

- Отчёт orphan: `artvision-data/audits/2026-05-29-repo-refactor/orphan-candidates.md`
- Strict-спека orders-state: `clients/blumart/orm/strict-publications-count-2026-05-23.md`
- Recap: `sync/recaps/6f79d39e-fcfa-4594-81f0-5a8e5c392106.md`
- Task #6 (deferred) в session task list.
