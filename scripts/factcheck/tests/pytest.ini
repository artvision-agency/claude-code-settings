[pytest]
markers =
    unit: fast deterministic unit tests (no network, no LLM)
    integration: tests that hit external services (Groq, real KP files)
filterwarnings =
    ignore::DeprecationWarning
