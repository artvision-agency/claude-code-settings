# AI Evolve — 2026-W17 (применённый отчёт)

**Дата:** 2026-04-26
**Источники:** 30 client patches (15 клиентов), context decisions, prev report 2026-W16

## Топ-5 паттернов
1. Pre-Task пропуск — ant-partners ×3 (18/24/28.02)
2. Strip/clean без regression-check — ant-partners ×2 (24.02, 03.03)
3. Валидация через grep вместо DOM — ant-partners ×3 (10.02, 24.02, 27.02)
4. Подмена шаблона/CSS-классов — ant-partners ×3
5. Чужой стиль скопирован — vaysman ×2, kp-brand DemosMed

## Применённые улучшения (7/7)

### 1. quality.md — Pre-Task Read Protocol gate
Добавлена секция "🗂 Любая задача по `clients/<name>/`" с шагом 0 (читать CLAUDE.md+patches+context-log) перед "📄 КП / HTML для клиента". Хук-якорь: `pre-client-work.sh`.

### 2. settings.json — расширение `pre-client-work.sh`
Хук уже существовал на Edit/Write. Попытка добавить на Bash — отменена (логика хука читает TOOL_INPUT_FILE_PATH, для Bash не применима). Текущее покрытие достаточное.

### 3. antipatterns.md — DOM не grep + baseline counts
Добавлены 2 строки в "Файлы и операции":
- "Валидация HTML/DOM через `grep`/`wc -l` (CSS в `<style>` ≠ DOM)" → BeautifulSoup/lxml
- "Валидатор без regression-проверки" → `.section-counts.json` baseline

### 4. core.md — strip/clean ужесточение
В секции "VPS и деплой" дополнена строка про массовые операции с конкретным правилом для strip/clean/fix_*.py: git stash + --dry-run + порог >10% upset.

### 5. self-corrections.md — Инцидент #10 + 2 хука в таблицу
Добавлен инцидент #10 "Strip/clean скрипт без regression-check" (ant-partners 24.02 + 03.03). В таблицу "Активные защитные хуки" — `pre-client-work.sh` (уже был) и новый `pre-strip-script-guard.sh`.

### 6. antipatterns.md — vaysman patterns
Добавлены 2 строки в "Данные и контент":
- "Копировать SVG/цвет одного бренда на UI другого" → Max=#0077FF, не WhatsApp
- "section:nth-child(even) для CTA/footer без !important"

### 7. quality.md — 🎨 Замена шаблона/CSS
Добавлена секция "🎨 Замена шаблона / CSS-класса в CMS клиента" после "🔗 Замена URL". Запрет менять template ID без approval (прецедент ant-partners /nashi-uslugi/ 18.02).

## Новый защитный хук
`~/.claude/hooks/pre-strip-script-guard.sh` — PreToolUse(Bash) блокирует python *strip*/*clean*/*fix_inline*/*fix_clean*.py на путях `clients/*/` без `--dry-run`. Тесты 4/4 PASS. Bypass: `STRIP_FORCE=1`.

## Что НЕ применено
Нет — все 7 предложений приняты пользователем (vote: a).

## Следующая итерация
- Через неделю — посмотреть, активировался ли `pre-strip-script-guard.sh` в реальной работе
- Если 0 активаций за месяц → защита написана впрок, оставляем
- Memory lint: 1 critical (orphan `feedback_no_internal_markers_in_client_docs.md`) — закрыть отдельной задачей
