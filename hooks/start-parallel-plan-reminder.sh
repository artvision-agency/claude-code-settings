#!/bin/bash
# SessionStart hook: в начале КАЖДОЙ сессии — напоминание составить ПЛАН задач с параллельным выполнением.
# Антон 29.05: «в каждой любой сессии, в начале, чтобы был план» + «не хочу просить про todo-лист».
# Правило: ~/.claude/rules/parallel-task-orchestration.md
# БЕЗОПАСНО: только инжект текста в stdout, exit 0 всегда, не блокирует старт.

set +e
[ "${PARALLEL_PLAN_OFF:-0}" = "1" ] && exit 0

cat <<'EOF'
═══════════════════════════════════════════
📋 ПЛАН СЕССИИ С ПАРАЛЛЕЛЬЮ (авто, правило parallel-task-orchestration.md)
═══════════════════════════════════════════
При получении задач в этой сессии — БЕЗ просьбы Антона:
1. TaskCreate на каждую подзадачу (видимый план)
2. Анализ зависимостей: независимые vs цепочка (addBlockedBy)
3. Независимые → Agent(run_in_background:true) РОЕМ сеньоров в одном
   сообщении, авто-подбор по agent-roster.md (макс 3-4, только Opus)
4. Зависимые → по готовности
НЕ выполнять последовательно если задачи независимы.
Bypass: PARALLEL_PLAN_OFF=1
═══════════════════════════════════════════
EOF
exit 0
